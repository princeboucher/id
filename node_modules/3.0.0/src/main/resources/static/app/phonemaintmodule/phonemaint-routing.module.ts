import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';

import { PhonemaintComponent } from './phonemaint.component';


@NgModule( {
    imports: [RouterModule.forChild( [{ path: 'customeractions/phonemaint', component: PhonemaintComponent }] )],
    exports: [RouterModule]
} )

export class PhoneMaintRouting { }

